{
  "entities": {
    "User": {
      "title": "User Profile",
      "description": "User account data including subscription and activity points",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "Firebase Auth UID" },
        "email": { "type": "string", "format": "email" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" },
        "subscriptionStatus": { 
          "type": "string", 
          "enum": ["free", "pro", "annual"],
          "description": "Current subscription tier" 
        },
        "plan": { 
          "type": "string", 
          "enum": ["free", "pro", "annual"],
          "description": "Subscription plan (alias for subscriptionStatus)" 
        },
        "points": { "type": "number", "description": "Loyalty or activity points" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "email", "subscriptionStatus", "points"]
    },
    "Analysis": {
      "title": "Predictive Analysis",
      "description": "Market assessments and probability estimations",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "category": { "type": "string" },
        "eventDate": { "type": "string", "format": "date-time" },
        "pMarket": { "type": "number", "description": "Market probability (0-100)" },
        "pReal": { "type": "number", "description": "Estimated true probability (0-100)" },
        "edge": { "type": "number" },
        "content": { "type": "string", "description": "Full markdown analysis text" },
        "summary": { "type": "string" },
        "status": { "type": "string", "enum": ["active", "resolved"] },
        "resolution": { "type": "string" },
        "isCorrect": { "type": "boolean" },
        "imageUrl": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "TrackRecordEntry": {
      "title": "Track Record Entry",
      "description": "A resolved prediction record",
      "type": "object",
      "properties": {
        "event": { "type": "string" },
        "date": { "type": "string" },
        "ourEst": { "type": "number" },
        "marketPrice": { "type": "number" },
        "edge": { "type": "number" },
        "resolution": { "type": "string" },
        "resolutionTimestamp": { "type": "string", "format": "date-time" },
        "outcome": { "type": "string" },
        "correct": { "type": "boolean" }
      }
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "Individual user profiles and points"
    },
    "/analysis/{analysisId}": {
      "schema": "Analysis",
      "description": "Collection of all market analyses"
    },
    "/trackRecord/{trackId}": {
      "schema": "TrackRecordEntry",
      "description": "Collection of all resolved prediction records"
    }
  }
}
